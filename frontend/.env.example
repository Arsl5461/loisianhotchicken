# Vite dev server API (used when VITE_BACKEND_URL is empty)
VITE_API_URL=http://localhost:5000/api/v1

# Backend origin. Change this in production, then rebuild the frontend.
# Example: https://api.yourdomain.com
VITE_BACKEND_URL=http://localhost:5000
